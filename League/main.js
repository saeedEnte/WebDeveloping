function mainLeague() {
    var num_of_teams = prompt("Please enter the number of teams","13");
    // var teams = num_of_teams[num_of_teams]

    for (let i = 0; i < num_of_teams.length; i++) {
        teams.push(new Object());
    }
}